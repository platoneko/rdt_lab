# RDT LAB

## Build

```sh
cmake . -DPROJECT:STRING=XXX
make
```

XXX can be WaitStop, GBN, SR or TCP;

default = WaitStop;

## Run

```sh
bin/XXX > XXX.log
```

XXX can be WaitStop, GBN, SR or TCP;
